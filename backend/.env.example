PORT=5000
NODE_ENV=development

# Email configuration (for contact form)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email
# SMTP_PASS=your-password

# Database (if needed)
# DATABASE_URL=mongodb://localhost:27017/nidourbano
